import { Link } from 'gatsby';
import React from 'react';

const Header: React.FC<{
  siteTitle: string;
}> = ({ siteTitle }) => <header></header>;

export default Header;

// {/* <Link to="/">{siteTitle}</Link> */}
